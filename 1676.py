n = int(input())

def search(l, q):
    while idx1 < idx2:
        mid = idx1 + (idx2 - idx1) // 2
        if (l[mid] == q):
            idx1 = mid
            idx2 = mid
            break
        elif (l[mid] > q):
            idx2 = mid - 1
        else:
for i in range(n):
    lag = list(map(int, input().split()))
    l = list(map(int, input().split()))
    for _ in range(lag[1]):
        q = int(input())
        idx1 = 0
        idx2 - len(l)

                idx1 = mid +1
        if (idx1 == idx2):
            print(0)
        elif(l[idx1] < q and l[idx2] > q):

